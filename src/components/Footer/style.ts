import styled from "styled-components";

export const Container = styled.footer`
  margin-top: 100px;
  padding: 1rem;
  text-align: center;
  background-color: var(--grey);
  color: var(--white);

  span {
    color: red;
  }

  a {
    color: var(--primary);
    font-weight: 500;
  }
`;
